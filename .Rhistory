source('~/.active-rstudio-document')
source('~/Desktop/ST2195 Assignment 4/foo.R')
# Run volume_vector(r) and print the volumes of the spheres with radius r, r^2 and r^3
volume_vector(r)
debugSource('~/Desktop/ST2195 Assignment 4/foo.R')
# Function to compute the volume of a sphere with radius r
volume <- function(r, rho) {
3/4*pi*r^2
}
dius
r <- 2
# Function to compute the volume of a sphere with radius r
volume <- function(r, rho) {
3/4*pi*r^2
}
# Function to compute the volumes of the spheres with radius r, r^2 and r^3
volume_vector <- function(r) {
r <- 22
for (r in 2:4){
volume(r)
}
}
# Run volume_vector(r) and print the volumes of the spheres with radius r, r^2 and r^3
volume_vector(r)
# Run volume_vector(r) and print the volumes of the spheres with radius r, r^2 and r^3
volume_vector(r)
volume_vector(r)
debugSource('~/Desktop/ST2195 Assignment 4/foo.R')
n
# Run volume_vector(r) and print the volumes of the spheres with radius r, r^2 and r^3
volume_vector(r)
source('~/Desktop/ST2195 Assignment 4/foo.R')
volumes
# Run volume_vector(r) and print the volumes of the spheres with radius r, r^2 and r^3
volume_vector(r)
# Run volume_vector(r) and print the volumes of the spheres with radius r, r^2 and r^3
volume_vector(r)
source('~/Desktop/ST2195 Assignment 4/foo.R')
source('~/Desktop/ST2195 Assignment 4/foo.R')
source('~/Desktop/ST2195 Assignment 4/foo.R')
volumes <- append(volumes, vol)
return(volumes)
# Run volume_vector(r) and print the volumes of the spheres with radius r, r^2 and r^3
volume_vector(r)
source('~/Desktop/ST2195 Assignment 4/foo.R')
#r <- 22
volumes <- list()
# Run volume_vector(r) and print the volumes of the spheres with radius r, r^2 and r^3
volume_vector(r)
# Run volume_vector(r) and print the volumes of the spheres with radius r, r^2 and r^3
volume_vector(r)
# Radius
r <- 2
# Function to compute the volume of a sphere with radius r
volume <- function(r) {
(4/3)*pi*(r^3)
}
# Function to compute the volumes of the spheres with radius r, r^2 and r^3
volume_vector <- function(r) {
#r <- 22
volumes <- c()
for (power in 1:3){
vol <- volume(r^power)
#volumes <- c(volumes, vol)
volumes <- append(volumes, vol)
}
return(volumes)
}
# Run volume_vector(r) and print the volumes of the spheres with radius r, r^2 and r^3
volume_vector(r)
# Run volume_vector(r) and print the volumes of the spheres with radius r, r^2 and r^3
volume_vector(r)
